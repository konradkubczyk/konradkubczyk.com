stages:
  - deploy

deploy-job:
  stage: deploy
  environment: production
  script:
    - echo "Deploying files using FTP..."
    - apt update -qq && apt install -qq lftp 
    - lftp -e "open -u $FTP_USERNAME,$FTP_PASSWORD $FTP_URL; mirror -R $LOCAL_DIR $REMOTE_DIR"
    - echo "Files deployed successfully."
